<launch>
  <!-- Nodo de cámara del tablero -->
  <node pkg="battleship_vision"
        type="board_camera_node.py"
        name="board_camera_node"
        output="screen" />

  <!-- Nodo del tablero -->
  <node pkg="battleship_vision"
        type="board_node.py"
        name="board_node"
        output="screen">
    <param name="image_topic" value="board_camera/image_raw" />
  </node>
</launch>
